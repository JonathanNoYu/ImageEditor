/**
 * a {@code LoadImage} loads the image from the specified path.
 */
public class LoadImage implements ImageCommand {

  @Override
  public int[] process(int... inputs) {

    return new int[0];
  }
}
